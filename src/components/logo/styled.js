import styled from 'styled-components'

export const TGLogo = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    margin: 40px 0;
    img {
        display: block;
        max-width: calc(100% - 20px);
        margin: 0 auto;
    }
`
